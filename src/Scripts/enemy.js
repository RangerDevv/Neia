const fire_enemy = {
    name: "Fire Enemy",
    health: 30,
    maxHealth: 30,
    attack: 10,
}

// get the player
import {player,textdialogue,choiceA,choiceB} from './game.js';

function fight() {
    
}